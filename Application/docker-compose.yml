version: '3.3'

services:
  eformprocessingservice:
    image: eformprocessingservice:${buildid}
    build:
      context: .
      dockerfile: Application/eFormProcessingService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
  loginapiservice:
    image: loginapiservice:${buildid}
    build:
      context: .
      dockerfile: Application/LoginAPIService/Dockerfile
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
